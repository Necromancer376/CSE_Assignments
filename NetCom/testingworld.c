#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char* CRC()
{

}

int main ()
{
    int div_size, data_size;
    char* data = (char*)malloc(32*sizeof(char));
    char* div = (char*)malloc(32*sizeof(char));
    char* final = (char*)malloc(64*sizeof(char));

    scanf("%d", &divSize)
    scanf("%s", div);
    scanf("%d", &dataSize)
    scanf("%s", data);

    for(int i=0; i<divSize; i++)
        data[i+dataSize] = '0';

    
}